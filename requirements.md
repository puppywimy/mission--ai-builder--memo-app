# Requirements

## 1. 앱 목적

이 앱은 사용자가 수업을 들으면서 메모를 빠르게 분류해 기록할 수 있는 메모 앱이다.

사용자는 수업 중 떠오르는 내용을 다음 세 가지 탭으로 나누어 작성할 수 있어야 한다.

1. 수업 메모
2. 질문
3. 빠른 메모

## 2. 탭 구성

앱에는 다음 세 가지 탭이 있어야 한다.

### 2.1 수업 메모 탭

* 수업을 들으면서 핵심 내용을 메모하는 공간이다.
* 기본으로 가장 먼저 보이는 탭이다.

### 2.2 질문 탭

* 수업을 들으면서 수업 후 강사에게 질문할 내용을 메모하는 공간이다.

### 2.3 빠른 메모 탭

* 수업과 직접 관련 없는 생각이나 할 일을 빠르게 적는 공간이다.

## 3. 빠른 탭 전환

* 사용자는 세 가지 탭을 빠르게 전환할 수 있어야 한다.
* 탭을 전환해도 각 탭에 작성 중이던 내용은 유지되어야 한다.
* 탭 전환 시 작성 중인 내용이 사라지면 안 된다.

## 4. 공통 메모 편집 방식

* 세 탭은 모두 동일한 편집 방식을 사용한다.
* 각 탭은 하나의 메모 문서처럼 동작한다.
* 사용자는 Notion과 유사하게 블록 단위로 메모를 작성할 수 있어야 한다.

## 5. 블록 기반 메모

* 메모는 여러 개의 블록으로 구성된다.
* 블록은 작성한 순서대로 위에서 아래로 표시되어야 한다.
* 블록 사이의 구분선은 사용자에게 보이지 않아야 한다.
* 메모에는 항상 최소 한 개의 블록이 유지되어야 한다.
* 블록 사이에는 약간의 간격을 둬, 블록 안의 줄바꿈과 블록 경계가 시각적으로 구분되어야 한다.

### 5.1 블록 추가

* 사용자는 새 블록을 추가할 수 있어야 한다.
* 블록에서 Enter를 누르면 해당 블록 바로 아래에 새 블록이 자동으로 추가되어야 한다.
* 블록 안에서 줄바꿈은 Shift + Enter로 입력할 수 있어야 한다.
* 블록 좌측에 마우스를 호버하면 + 버튼이 나타나야 한다.
* + 버튼을 누르면 해당 블록 바로 아래에 새 블록이 추가되어야 한다.
* Enter 또는 + 버튼으로 새 블록을 추가하면, 추가된 블록으로 포커스가 이동해 바로 이어서 입력할 수 있어야 한다.
* 메모의 빈 공간을 클릭하면 가장 아래 블록으로 포커스가 이동해야 한다. 단, 가장 아래 블록에 내용이 있으면 새 빈 블록을 추가하고 그 블록으로 포커스가 이동해야 한다.

### 5.2 블록 수정

* 사용자는 기존 블록의 내용을 수정할 수 있어야 한다.

### 5.3 블록 삭제

* 사용자는 블록을 삭제할 수 있어야 한다.
* 블록 좌측에 마우스를 호버하면 휴지통 아이콘이 나타나야 한다.
* 휴지통 아이콘을 누르면 해당 블록이 삭제되어야 한다.
* 빈 블록에서 Backspace를 누르면 해당 블록이 삭제되고 이전 블록으로 포커스가 이동해야 한다. 단, 첫 블록은 삭제되지 않는다.
* 블록 좌측의 + 버튼과 휴지통 아이콘은 호버 시 부드럽게 나타나야 한다.

## 6. 지원하는 블록 및 서식

### 6.1 일반 텍스트

* 기본 블록 타입이다.
* 사용자는 자유롭게 텍스트를 입력할 수 있어야 한다.

### 6.2 제목

* 제목 1, 제목 2, 제목 3을 지원해야 한다.
* 각 제목은 서로 다른 시각적 크기와 강조를 가져야 한다.
* 사용자가 블록 시작 부분에 # 를 입력하면 제목 1 블록으로 전환되어야 한다.
* 사용자가 블록 시작 부분에 ## 를 입력하면 제목 2 블록으로 전환되어야 한다.
* 사용자가 블록 시작 부분에 ### 를 입력하면 제목 3 블록으로 전환되어야 한다.

### 6.3 볼드체

* 사용자는 텍스트 일부를 볼드체로 표시할 수 있어야 한다.

### 6.4 리스트

* 사용자가 블록 시작 부분에 - 를 입력하면 리스트 블록으로 전환되어야 한다.
* 사용자가 블록 시작 부분에 * 를 입력하면 리스트 블록으로 전환되어야 한다.
* 리스트 블록은 bullet list 형태로 표시되어야 한다.

### 6.5 화살표 자동 변환

* 사용자가 ->를 입력하면 자동으로 →로 변환되어야 한다.

### 6.6 인라인 코드

* 사용자가 텍스트를 백틱으로 감싸면 해당 텍스트는 인라인 코드 형태로 표시되어야 한다.
* 사용자는 Command + E 단축키로도 선택한 텍스트를 인라인 코드로 전환할 수 있어야 한다.

### 6.7 코드 블록

* 사용자가 백틱 세 개를 입력하면 코드 블록을 삽입할 수 있어야 한다.
* 코드 블록에는 여러 줄의 코드를 입력할 수 있어야 한다.
* 코드 블록의 언어 설정 기능은 구현하지 않는다.

## 7. 블록 순서 변경

* 사용자는 각 블록을 드래그 앤 드롭으로 이동할 수 있어야 한다.
* 블록을 이동하면 해당 탭 안에서 블록의 순서가 변경되어야 한다.
* 블록 순서를 변경해도 블록의 내용과 서식은 유지되어야 한다.

## 8. Markdown 공유

* 화면 우측 상단에 공유하기 아이콘이 있어야 한다.
* 사용자가 공유하기 아이콘을 클릭하면 현재 선택된 탭의 내용을 Markdown 형식으로 변환해야 한다.
* 변환된 Markdown 텍스트는 클립보드에 복사되어야 한다.
* 복사가 완료되면 사용자에게 복사 완료 상태를 알려야 한다.

### 8.1 Markdown 변환 규칙

* 일반 텍스트 블록은 일반 문장으로 변환한다.
* 제목 1 블록은 # 제목 형식으로 변환한다.
* 제목 2 블록은 ## 제목 형식으로 변환한다.
* 제목 3 블록은 ### 제목 형식으로 변환한다.
* 리스트 블록은 - 항목 형식으로 변환한다.
* 볼드체는 **텍스트** 형식으로 변환한다.
* 인라인 코드는 백틱 한 쌍으로 감싼다.
* 코드 블록은 백틱 세 개로 감싸진 Markdown 코드 블록으로 변환한다.
* 화살표는 변환된 → 문자를 그대로 유지한다.

## 9. 제외 기능

다음 기능은 이번 구현 범위에서 제외한다.

* 이미지 업로드
* 코드 블록 언어 설정